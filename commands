git config --global user.email "binayraj@gmail.com"
git config --global user.name "binayraj"

Learn git Part - 1 :

1) git init
2) git add {file name } || -A
3) git status
4) git commit -m "msg"
5) git remote add origin RepoName
6) git push origin master
7) git clone RepoName
8) git pull origin master

